# סיכום שדרוגים - גרסה 1.2.0

## שדרוגי הקראה קולית

- `speechService.ts` שודרג עם ניקוי טקסט לפני הקראה, בחירת קול עברי מועדף ומניעת חזרתיות מהירה.
- `useSpeech.ts` הורחב עם `getSpeakProps` כדי לאפשר הקראה ב-Hover וב-Focus בכל רכיב רלוונטי.
- כרטיסי משחק, כפתורים, אפשרויות תשובה, פריטי התאמה, קלפי זיכרון, רצפים וסלי מיון תומכים בהקראה.
- נוסף `gameInstructions.ts` עם הוראות פתיחה לכל משחק.

## שדרוגי לובי וגרפיקה

- `LandingPage.tsx` נבנה מחדש כלובי עשיר ומזמין יותר.
- נוספו אנימציות CSS, Mascot, Sparkles, משחקים מקדימים, כרטיסים משודרגים ותמיכה ב-reduced motion.
- `GameCard.tsx` שודרג לכרטיסים עשירים עם תנועה, Shine ותגובות Hover/Focus.

## שדרוג מאגר שאלות

- הורחבו קבצי השאלות:
  - `letters.questions.ts`
  - `numbers.questions.ts`
  - `shapes.questions.ts`
  - `colors.questions.ts`
- המאגר גדל משמעותית וממשיך להיות מפולח לפי גיל ורמת קושי.

## סוכן התאמת שאלות בסגנון קלינאית תקשורת

- נוסף `speechTherapistAgent.ts` שבודק:
  - עברית ברורה.
  - אורך שאלה מתאים לגיל.
  - קיום תשובה נכונה.
  - מניעת כפילויות באפשרויות.
  - התאמת רמת קושי לגיל.
- `questionService.ts` מחזיר רק שאלות שעברו אישור בסיסי.

## משחקים חדשים

- `PatternGame.tsx` - משחק רצפים.
- `SortingGame.tsx` - משחק מיון וסיווג.
- נוספו טיפוסים חדשים ב-`question.types.ts` ודאטה חדש ב-`activityData.ts`.

## אבטחת מידע

- לא בוצעה קריאת GPT Image ישירות מה-Frontend, כדי לא לחשוף API Key בדפדפן.
- הגרפיקה והאנימציות הוטמעו כנכסי CSS/SVG סטטיים.
- אם רוצים בעתיד יצירת תמונות אוטומטית עם GPT Image, מומלץ להוסיף Backend מאובטח שמחזיק את המפתח ומחזיר נכסים סטטיים ל-Frontend.

## בדיקה

- בוצע TypeScript build ו-Vite production build בהצלחה לפני ניקוי `node_modules` מהחבילה.
- `node_modules` לא כלול ב-ZIP הסופי. להרצה יש לבצע `npm install` ואז `npm run dev`.
